
<%= render 'workshops/filter_bar' %>

<div class="container mt-4">
  <div class="row">
    <div class="col-12">
       <div class="index-grid">
        <% @workshops.each do |workshop| %>
          <%= render 'workshops/card', workshop: workshop %>
        <% end %>
      </div>
    </div>
  </div>
</div>

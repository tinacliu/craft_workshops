
    <div class="card" style="width: 18rem;">
      <%= cl_image_tag workshop.photos.first.image, crop: :fill, class: 'card-img-top' %>
      <div class="card-body">

        <p class="card-subtitle"><%= workshop.category %></p>
        <h6 class="card-title mt-2"><%= link_to workshop.name, workshop_path(workshop), class: "card-link"%></h6>
<!--         <h5 class="card-title mt-2"><%#= truncate(workshop.name,
                   omission: "", length: 40,  separator: " ")%></h5> -->
      </div>
      <div class="card-footer d-flex justify-content-around s_font">
        <div class="card-foot-square">
          <i class="fas fa-yen-sign mr-2"></i><%= workshop.price %>
        </div>
        <div class="card-foot-square">|</div>
        <div class="card-foot-square">
          <i class="fas fa-signal mr-2"></i><%= workshop.level %>
        </div>
        <div class="card-foot-square">|</div>
        <div class="card-foot-square">
          <i class="far fa-clock mr-2"></i><%= "#{workshop.duration} mins" %>
        </div>
    </div>
  </div>

